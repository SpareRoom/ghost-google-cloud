apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: ghost-uk
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: ghost-uk
    spec:
      containers:
      - name: ghost-uk
        image: spareroom/ghost-blog-uk:48
        ports:
        - containerPort: 2368
        imagePullPolicy: Always
        volumeMounts:
          - name: cloud-storage-credentials
            mountPath: /secrets/cloudstorage
            readOnly: true
        env:
        - name: GHOST_URL
          value: "http://blog.spareroom.co.uk"
        - name: GHOST_DB
          value: "ghost-spareroom-uk"
        - name: GHOST_ASSETS_PROJECT
          value: "spareroom-blogs-uk"
        - name: GHOST_ASSETS_BUCKET
          value: "ghost-spareroom-uk-assets"
        - name: GHOST_PASSWORD
          valueFrom:
            secretKeyRef:
              name: spareroomuksqldb
              key: password
        - name: GHOST_USER
          valueFrom:
            secretKeyRef:
              name: spareroomuksqldb
              key: username
        - name: GHOST_DB_HOST
          value: "127.0.0.1"
        - name: GHOST_EMAIL_FROM
          value: "no-reply@spareroom.co.uk"
        - name: GHOST_EMAIL_KEY
          valueFrom:
            secretKeyRef:
              name: spareroomukemail
              key: key
        - name: GHOST_EMAIL_SECRET
          valueFrom:
            secretKeyRef:
              name: spareroomukemail
              key: secret
        - name: GHOST_EMAIL_REGION
          value: "eu-west-1"
      - image: gcr.io/cloudsql-docker/gce-proxy:1.09
        name: cloudsql-proxy
        command: ["/cloud_sql_proxy", "--dir=/cloudsql",
                    "-instances=spareroom-blogs:europe-west2:ghost=tcp:3306",
                    "-credential_file=/secrets/cloudsql/credentials.json"]
        volumeMounts:
          - name: cloudsql-instance-credentials
            mountPath: /secrets/cloudsql
            readOnly: true
          - name: ssl-certs
            mountPath: /etc/ssl/certs
          - name: cloudsql
            mountPath: /cloudsql
      imagePullSecrets:
        - name: spareroomdockercredentials
      volumes:
        - name: cloudsql-instance-credentials
          secret:
            secretName:
                spareroomuksql
        - name: cloud-storage-credentials
          secret:
            secretName:
                spareroomukassets
        - name: ssl-certs
          hostPath:
            path: /etc/ssl/certs
        - name: cloudsql
          emptyDir:
