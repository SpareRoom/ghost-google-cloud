apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: ghost
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: ghost
    spec:
      containers:
      - name: ghost
        image: spareroom/ghost-google-cloud:43
        ports:
        - containerPort: 2368
        imagePullPolicy: Always
        volumeMounts:
          - name: cloud-storage-credentials
            mountPath: /secrets/cloudstorage
            readOnly: true
        env:
        - name: GHOST_DB
          value: "ghost-spareroom-us"
        - name: GHOST_ASSETS_PROJECT
          value: "spareroom-blogs"
        - name: GHOST_ASSETS_BUCKET
          value: "ghost-spareroom-us-assets"
        - name: GHOST_PASSWORD
          valueFrom:
            secretKeyRef:
              name: spareroomussqldb
              key: password
        - name: GHOST_USER
          valueFrom:
            secretKeyRef:
              name: spareroomussqldb
              key: username
        - name: GHOST_DB_HOST
          value: "127.0.0.1"
      - image: gcr.io/cloudsql-docker/gce-proxy:1.09
        name: cloudsql-proxy
        command: ["/cloud_sql_proxy", "--dir=/cloudsql",
                    "-instances=spareroom-blogs:europe-west1:ghost-db=tcp:3306",
                    "-credential_file=/secrets/cloudsql/credentials.json"]
        volumeMounts:
          - name: cloudsql-instance-credentials
            mountPath: /secrets/cloudsql
            readOnly: true
          - name: ssl-certs
            mountPath: /etc/ssl/certs
          - name: cloudsql
            mountPath: /cloudsql
      volumes:
        - name: cloudsql-instance-credentials
          secret:
            secretName:
                spareroomussql
        - name: cloud-storage-credentials
          secret:
            secretName:
                spareroomusassets
        - name: ssl-certs
          hostPath:
            path: /etc/ssl/certs
        - name: cloudsql
          emptyDir:
